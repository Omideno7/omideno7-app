Omideno7 V63.70 — Stable Admin Request List

این فایل فقط مشکل بسته‌شدن سریع لیست «بارگذاری درخواست‌ها» را اصلاح می‌کند.

Upload / Replace:
- فقط فایل v63-63-admin-registration-approval-beta.js را جایگزین کن.

اصلاح انجام‌شده:
- کارت ادمین دیگر بعد از بارگذاری لیست، دوباره innerHTML نمی‌سازد.
- لیست درخواست‌ها بعد از کلیک روی «بارگذاری درخواست‌ها» باز می‌ماند.
- دکمه «تأیید و فعال‌سازی کد جلسه» قابل کلیک می‌ماند.
- فقط omideno7church@gmail.com ادمین است.
- هیچ بخش دیگری از اپ تغییر نمی‌کند.

نیازی به SQL جدید نیست؛ مگر اینکه قبلاً SQL توابع ادمین را اجرا نکرده باشی.
