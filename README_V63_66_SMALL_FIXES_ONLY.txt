Omideno7 V63.66 — Small Fixes Only

این ZIP فقط دو فایل جایگزین دارد و هیچ بخش دیگری از اپ را تغییر نمی‌دهد.

Upload / Replace فقط همین دو فایل:
- v63-62-meeting-approval-strict-beta.js
- v63-64-faith-declaration-daily-fix-beta.js

اصلاحات:
1) در پیام «ثبت‌نام هنوز تأیید نشده است»، کد جلسه دیگر نمایش داده نمی‌شود.
2) در اعلان‌های ایمان، وقتی روی آیه کلیک می‌شود، آیه باز می‌ماند و فوراً بسته نمی‌شود.

نیازی به SQL جدید نیست.
نیازی به تغییر beta.html نیست.
