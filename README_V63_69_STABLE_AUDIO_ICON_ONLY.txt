Omideno7 V63.69 — Stable Spiritual Audio Speaker Icon

این نسخه فقط مشکل پرش/محو شدن آیکون بلندگو کنار «پیام‌های صوتی روحانی» را اصلاح می‌کند.

Upload / Replace:
- فقط فایل v63-60-remove-duplicate-welcome-amen-beta.js را جایگزین کن.

چه چیزی عوض شد:
- قبلاً آیکون با innerHTML به متن اضافه می‌شد و هنگام رندر مجدد صفحه پرش داشت.
- اکنون فقط یک class به عنوان کارت اضافه می‌شود و آیکون با CSS ::before نمایش داده می‌شود.
- MutationObserver اضافه شد تا اگر Home دوباره رندر شد، class سریع و بدون پرش برگردد.

نیازی به تغییر beta.html نیست.
نیازی به SQL نیست.
هیچ بخش دیگری تغییر نمی‌کند.
