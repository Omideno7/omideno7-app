Omideno7 V63.80 — More Page Final Fixes

این نسخه فقط روی صفحه «بیشتر» کار می‌کند و این موارد را اصلاح می‌کند:
1) اگر «ارتباط با ما» خالی باشد، ایمیل، اینستاگرام و یوتیوب کلیسا را برمی‌گرداند.
2) اگر بخش «هدایت و حمایت» کلیدهای PayPal و Revolut نداشته باشد، آنها را اضافه می‌کند.
3) نوارهای رنگی باقی‌مانده بالای کارت مدال‌ها را مخفی می‌کند.
4) کارت «گزارش تست نسخه بتا» را مخفی می‌کند.
5) راهنمای مدال‌ها را پایدار می‌کند تا سریع بسته نشود.

Upload:
1) فایل v63-80-more-final-fixes-beta.js را در GitHub آپلود کن.
2) در beta.html بعد از آخرین فایل V63 و قبل از </body> این خط را اضافه کن:
<script src="v63-80-more-final-fixes-beta.js?v=6380"></script>

اگر همین اصلاحات در نسخه اصلی لازم بود، همین خط را در index.html هم قبل از </body> اضافه کن.

نکته مهم:
لینک‌های پیش‌فرض حمایت داخل فایل:
PAYPAL_URL='https://paypal.me/omideno7'
REVOLUT_URL='https://revolut.me/omideno7'
اگر لینک واقعی کلیسا متفاوت است، فقط همین دو خط بالای فایل را تغییر بده.

هیچ بخش دیگری از اپ تغییر نمی‌کند.
SQL لازم نیست.
