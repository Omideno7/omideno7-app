Omideno7 V63.49d — Home + Salvation + Registration + Fasting Fix Beta

این نسخه طبق درخواست‌های جدید اصلاح می‌کند:

1. عنوان فرم ثبت‌نام:
   فقط «فرم ثبت‌نام» نمایش داده می‌شود.

2. متن اشتباه «اگر موردی ندارید بنویسید ندارم» حذف شد.
   به جای آن تأکید می‌شود که اطلاعات دقیق و صحیح وارد شود.

3. فرم ثبت‌نام از منوی «بیشتر» حذف/مخفی می‌شود.

4. کلید «خلاصه مدال‌ها» به «راهنمای مدال‌ها» تغییر می‌کند
   و با کلیک، باز/بسته شدنش پایدارتر می‌شود.

5. پیام کوتاه روزانه در خانه اضافه می‌شود
   و هنگام باز شدن اپ هم یک پنجره کوچک با دکمه «آمین» نمایش داده می‌شود.

6. بخش «تولد تازه و نجات در مسیح» فقط در خانه و در جای درست خودش نمایش داده می‌شود.
   دکمه‌ها به ترتیب:
   - نیاز به نجات / ثبت‌نام
   - نجات چیست؟
   - دعای نجات
   - زندگی مسیحی چیست؟
   - ویدیوهای تولد تازه
   - ثبت‌نام

7. فقط دکمه ثبت‌نام فرم داخل اپ را باز می‌کند.

8. در بخش پلن‌ها/روزه، تعلیم جدید روزه اضافه شد
   و آیات به شکل دکمه قابل کلیک قرار گرفتند.

فایل‌هایی که باید Upload/Replace شوند:
- beta.html
- v63-49d-home-salvation-registration-fasting-fix-beta.js
- README_V63_49D_HOME_SALVATION_FASTING_FIX_BETA.txt
- OMIDENO7_APP_STATUS_REPORT.md

SQL جدید لازم نیست.
لینک تست:
https://omideno7.github.io/omideno7-app/beta.html?v=6349d
